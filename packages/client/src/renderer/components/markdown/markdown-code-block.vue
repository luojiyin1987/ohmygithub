<script setup lang="ts">
import { computed } from 'vue'
import type { MarkdownCodeNode } from './markstream-node-types'
import ShikiCode from '@/components/code/shiki-code.vue'

const props = defineProps<{
  node: MarkdownCodeNode
}>()

const code = computed(() => props.node.code ?? props.node.content ?? props.node.raw ?? '')
const language = computed(() => props.node.language ?? 'plaintext')
</script>

<template>
  <div class="my-3 overflow-hidden rounded-lg">
    <ShikiCode
      :code="code"
      :language="language"
      :padded="true"
      :themed-background="true"
    />
  </div>
</template>
